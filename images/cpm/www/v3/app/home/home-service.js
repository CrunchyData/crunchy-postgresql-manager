angular.module('uiRouterSample.home.service', [])

// A RESTful factory for retrieving home from 'home.json'
.factory('home', ['$http', 'utils', function($http, utils) {

    var factory = {};
    return factory;
}]);
